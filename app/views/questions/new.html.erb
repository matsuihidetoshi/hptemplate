<div class="text-center">
  <h1>お問い合わせ</h1>
  <div>弊社へのお問い合わせは、下記フォームに</div>
  <div>必要事項をご入力いただき</div>
  <div>「問い合わせ」ボタンを押してください</div>
  <div style="color:red;">「*」のついている項目は必須です。</div>
</div>

<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <%= form_for(@question) do |f| %>

      <%= render 'layouts/error_messages', model: f.object %>

      <div class="form-group">
        <span style="color:red;">*</span><%= f.label :name, 'お名前' %>
        <%= f.text_field :name, class: 'form-control' %>
      </div>

      <div class="form-group">
        <span style="color:red;">*</span><%= f.label :email, 'メールアドレス' %>
        <%= f.email_field :email, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :tel, 'お電話番号' %>
        <%= f.text_field :tel, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :address, 'ご住所' %>
        <%= f.text_field :address, class: 'form-control' %>
      </div>

      <div class="form-group">
        <span style="color:red;">*</span><%= f.label :title, '件名' %>
        <%= f.text_field :title, class: 'form-control' %>
      </div>

      <div class="form-group">
        <span style="color:red;">*</span><%= f.label :content, 'お問い合わせ内容' %>
        <%= f.text_area :content, {class: 'form-control', style: 'height:180px;'} %>
      </div>

      <%= f.submit '問い合わせ', class: 'btn btn-primary btn-block' %>
    <% end %>
  </div>
</div>
